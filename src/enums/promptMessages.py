
from enum import Enum, auto

class PromptMessages(Enum):
    CHAT_PROTOCOL = auto()
    VOTE_FOR_KILLING = auto()
    CHOOSE_A_CHANCELLOR_CANDIDATE = auto()
    CHANCELLOR_OPTIONS = auto()
    PROMPT = auto()
    EVERYTHING_SAID = auto()
    RECENT_MESSAGES = auto()